version: 2.1

description: >
  The 'secrets-manager' orb allows users to search for secrets stored
  in the organization projects (environment variables, project-level API tokens, SSH keys),
  as well as, in their organization contexts (environment variables).

display:
#  home_url: "https://github.com/orbiously/secrets-manager-orb"
  source_url: "https://github.com/orbiously/secrets-manager-orb"

# If your orb requires other orbs, you can import them like this. Otherwise remove the "orbs" stanza.
orbs:
  jq: circleci/jq@2.2.0
